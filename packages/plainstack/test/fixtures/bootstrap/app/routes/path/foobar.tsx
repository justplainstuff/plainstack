import { defineHandler } from "../../../../../../src/web/handler";

export const POST = defineHandler(async () => {
  return { status: 200 };
});

export const GET = defineHandler(async () => {
  return { status: 200 };
});
