export function PrinciplesSection() {
  // separation by technologies -> locality of behavior
  // serverless/edge -> long-running process
  // postgres/redis -> a single process
  // framework -> recipes
  // frontend build process -> no frontend build process
  return <div>todo</div>;
}
