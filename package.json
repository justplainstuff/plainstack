{
  "name": "plainweb-project",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "test": "turbo test",
    "cs": "changeset",
    "pub": "turbo run build lint test && changeset version && changeset publish",
    "check": "check --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^1.8.3",
    "@changesets/cli": "^2.27.3",
    "prettier": "^3.2.5",
    "turbo": "1.13.4",
    "typescript": "^5.3.3"
  },
  "packageManager": "pnpm@8.25.8",
  "engineStrict": true,
  "engines": {
    "node": ">=20.10.0"
  }
}
